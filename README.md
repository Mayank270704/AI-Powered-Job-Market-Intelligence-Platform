# AI Job Market Intelligence

This project is organized to support data collection, analysis, and reporting for AI job market insights.

## Suggested structure
- `data/raw` for raw datasets
- `data/processed` for cleaned datasets
- `notebooks` for exploratory analysis
- `src` for reusable code
- `scripts` for automation tasks
- `tests` for automated tests
- `docs` for documentation
